<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" >
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" ></script>
    <link href="css/style.css" rel="stylesheet"/>   
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">

</head>
<body>
   <div class="d-flex justify-content-between mt-2">
    <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal" class="btn" align="center">
      Add Income 
       </button>
       <a  href='/logout' type="button" class="btn btn-primary" class="btn">
         logout
       </a>
   </div>
    <h2 align="center">Budget Tracker</h2>

  <h2 align="center">Total Income : <span style=" font-weight:bold;"> <%= showincome?.addincome %> </span>
  
   
  </h2>
  <h2 align="center">Remaining Budget : <span style=" font-weight:bold;"><% let remain = showincome?.addincome - totalexpense%> <%= remain%> </span> </h2>

    <!-- Button trigger modal -->
  
  <!-- Modal -->
  <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h1 class="modal-title fs-5" id="exampleModalLabel">ADD INCOME</h1>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form method="post" action="/budget/budgetAdd" class="budget">
            <table align="center">
                      <tr>
                          <th>Total Income:</th>
                          <th><input type="text" name="addincome"/></th>
                          <th>
                            <input type="submit" name="submit" value="continue"/>
                            
                          </th>
                        </tr>
                      </table>
                      
            </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
         
        </div>
      </div>
    </div>
  </div>

  <h6 align="center">Add your expenses here....</h6>
  <form method="post" action="/budget/Addexpense">
        <table align="center">
                <tr>
                   
                    <td><input type="text" name="title" placeholder="expense title"/></td>
                    <td><input type="number" name="price" placeholder="expense price"/></td>
                    <td><input type="submit" name="submit" value="Add expense"/></td>
                </tr>
        </table>
  </form>

 <div class="d-flex" style="margin-top: 70px;">
    <div style="width:40%" class="d-flex">
      <div style="height:300px; width:200px; margin-left:40px;" >
        <canvas id="myChart"></canvas>
      </div>
      
      <div style="height:300px; width:200px; margin-left:40px;" >
        <canvas id="myChart1"></canvas>
      </div>
    </div>
  



  <div style="width:60%" >
     <table class="table table-striped" style="width:800px;" >
        <tr>
            <th>no</th>
            <th>title</th>
            <th>expense</th>
            <th>Action</th> 
            <th>Action</th> 
        </tr>

            <% showexpense.map((v,i)=>{ %>
                

                <tr>
                    <td><%= ++i%></td>
                    <td><%= v.title %></td>
                    <td><%= v.price %></td>
                    <td>
                      <a href="/budget/deldata/?id=<%= v.id %>"><i class="ri-delete-bin-6-line" style="color:red"></i></a> 
                    </td>
                    <td>
                      <a href="/budget/updateexp/?id=<%= v.id %>"><i class="ri-edit-2-fill" style="color:red"></i></a> 
                    </td>
                </tr>
            <% })%>
      </table>
        
  </div>
 </div>
  
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <script>
    const ctx = document.getElementById('myChart');
    

    let labeln =  '<%- JSON.stringify(expenselabel) %>'
    console.log(JSON.parse(labeln))

    
    let pricen =  '<%- JSON.stringify(pricevalue) %>'
    console.log(JSON.parse(pricen))

  
    new Chart(ctx, {
      type: 'pie',
      data: {
        labels:JSON.parse(labeln),
        datasets: [{
          label: '# of Votes',
          data: JSON.parse(pricen),
          borderWidth: 1
        }]
      },
      options: {
        scales: {
          y: {
            beginAtZero: true
          }
        }
      }
    });

    const ctx1 = document.getElementById('myChart1');

let labeln1 =  '<%- JSON.stringify(expenselabel) %>'
console.log(JSON.parse(labeln))


let pricen1 =  '<%- JSON.stringify(showincome) %>'
console.log(JSON.parse(pricen))


new Chart(ctx1, {
  type: 'doughnut',
  data: {
    labels:JSON.parse(labeln1),
    datasets: [{
      label: '# of Votes',
      data: JSON.parse(pricen),
      borderWidth: 1
    }]
  },
  options: {
    scales: {
      y: {
        beginAtZero: true
      }
    }
  }
});

  </script>



 
    
    
</body>
</html>